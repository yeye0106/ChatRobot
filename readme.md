# 多轮对话助手（LLM纯前端应用）作业整理版
我已按你的选择**多轮对话助手**，把全部要求整理成清晰、可直接照着做的清单，无多余内容。

---

## 一、核心技术要求
- 纯前端：**HTML + CSS + JavaScript** 静态文件
- 无后端、无数据库、无登录、无服务器转发
- 用 AI 编程工具：Cursor/ChatGPT/通义灵码/Trae 等
- 模型：**qwen3.6-plus**（阿里云百炼）
- 接口：阿里云百炼 OpenAI 兼容接口
- 北京地域 BASE_URL：
  `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions`

---

## 二、必须实现的功能（缺一不可）
1. 清晰**页面标题** + **使用说明**
2. **API Key 输入框**（用户自己填）
3. **用户输入区**（发消息）
4. **发送/生成按钮**
5. **对话历史展示区**
6. **加载中提示**（请求时显示）
7. **错误提示**（Key 错/网络错/接口报错）
8. **清空对话/重新开始**按钮
9. **多轮对话**：每次请求带上**历史消息**，让模型懂上下文

---

## 三、密钥安全要求（非常重要）
- **严禁**把 API Key 写进 HTML/JS/提交文件
- **严禁**上传、提交到 GitHub、公开显示
- 只允许两种方式：
  1. 用户**手动输入**自己的阿里云百炼 API Key
  2. 用 Supabase 等免费数据库安全存储（不强制）
- 可暂存浏览器内存，不可持久化到本地存储泄露

---

## 四、部署要求
- 必须上线，可公开访问
- 二选一：
  1. **GitHub Pages**
  2. **阿里云 OSS 静态网站托管**
- 最终链接必须是 **HTTPS**
- 域名可用默认 github.io 或自定义域名

---

## 五、提交内容（填课程群表格）
- 姓名
- 学号
- 项目名称（如：多轮对话助手）
- **HTTPS 可访问项目链接**

---

## 六、评分重点
- 应用功能完整
- 多轮对话上下文正常
- 页面体验友好
- 部署成功、HTTPS 正常
- 密钥安全、无泄露

---

需要我直接帮你**生成可直接使用的多轮对话助手完整代码**（HTML+CSS+JS）吗？复制即可部署。